//	cleanSlate.ns
//	by OKNewman
//
// 	Purpose: Deletes all NS2 scripts on localhost.
//	
//	Runs: Once, then stops.
//
//	Recommended for: Hacked servers, to clear old, erroneous scripts before adding current scripts

/** @param {NS} ns **/
export async function main(ns) {
	//if ns.getHostname()=='home';
	//	ns.tprint("ABORTING: Script is running on home.");
	//	ns.exit();
	//
	//get list of *.ns files on localhost
	//
	//
	//for(each ns_file in list)
	//	kill(ns_file);
	//	rm(ns_file);
	//
}